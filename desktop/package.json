{
  "name": "intel-hub-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "情报订阅站桌面客户端（Tauri + Next.js）",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:unsigned": "tauri build --no-bundle"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
